{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Battery SipJuice 默认权限：核心窗口 + 通知",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
